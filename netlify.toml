[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# SvelteKit's adapter-netlify emits functions automatically; this just
# documents the directory it uses.
[functions]
  node_bundler = "esbuild"
